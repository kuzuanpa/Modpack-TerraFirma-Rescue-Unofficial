This folder is to save sha1 lists of TFRU necessary files to validate TFRU. create any files named SkipCheck could skip this check(no suffix name, Case Sensitivity)
本文件夹用于存放TFRU完整性校验的sha1文件列表，在本目录新建任意名为SkipCheck的文件(没有后缀名，大小写敏感)即可跳过完整性校验
